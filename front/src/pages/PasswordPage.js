import PasswordContainer from '../common/containers/password';
import Layout from './Layout';

const PasswordPage = () => {
    return (
        <Layout>
            <PasswordContainer />
        </Layout>
    );
};

export default PasswordPage;
