import MainContainer from "../containers/main";
import Layout from "./Layout";

const MainPage = () => {
  return (
    <Layout>
      <MainContainer />
    </Layout>
  );
};

export default MainPage;
