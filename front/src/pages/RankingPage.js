import RankingContainer from '../containers/ranking';
import Layout from './Layout';

const RankingPage = () => {
  return (
    <Layout>
      <RankingContainer />
    </Layout>
  );
};

export default RankingPage;
