import UserContainer from "../containers/user";
import Layout from "./Layout";

const UserPage = () => {
  return (
    <Layout>
      <UserContainer />
    </Layout>
  );
};

export default UserPage;
