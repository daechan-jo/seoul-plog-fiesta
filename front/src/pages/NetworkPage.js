import NetworkContainer from '../containers/network';
import Layout from './Layout';

const NetworkPage = () => {
  return (
    <Layout>
      <NetworkContainer />
    </Layout>
  );
};

export default NetworkPage;
