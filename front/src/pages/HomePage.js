import HomeContainer from '../common/containers/home';
import Layout from './Layout';

const HomePage = () => {
  return (
    <Layout>
      <HomeContainer />
    </Layout>
  );
};

export default HomePage;
